
#!/bin/bash

for file in $(find ${SOURCE_ROOT} -maxdepth 1 -type d ); do
	cd ${file}
	if [[ -e .git ]]; then
		stat=$(git status -s)
		if [[ ! -z "${stat}" ]]; then
			echo "Repository ${file}" | color highcontrast " .*"
			git stash list 
			echo ""
		fi
	fi
done

